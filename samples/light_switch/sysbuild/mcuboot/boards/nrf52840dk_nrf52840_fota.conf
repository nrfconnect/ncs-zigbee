#
# Copyright (c) 2026 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

# MCUboot fprotect fails on nRF52840 with ext-flash FOTA layout (ACL region setup).
CONFIG_FPROTECT=n

# Workaround: disable memory guard to avoid false faults after boot (see nRF54L15 mcuboot FOTA).
CONFIG_HW_STACK_PROTECTION=n

CONFIG_NORDIC_QSPI_NOR=y
CONFIG_NORDIC_QSPI_NOR_FLASH_LAYOUT_PAGE_SIZE=4096
CONFIG_NORDIC_QSPI_NOR_STACK_WRITE_BUFFER_SIZE=16

# LZMA decompression needs a larger stack (see NCS smp_svr / compressed_update mcuboot).
CONFIG_MAIN_STACK_SIZE=16384
